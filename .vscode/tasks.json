{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Development",
      "type": "shell",
      "group": "build",
      "command": "lune run scripts/dev.luau",
      "isBackground": false,
      "runOptions": {
        "runOn": "folderOpen"
      },
      "options": {
        "env": {
          "PATH": "${env:HOME}/.rokit/bin:${env:PATH}"
        }
      }
    }
  ]
}
